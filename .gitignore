# ignore per-directory local state (tfstate should always be in s3, and module directories
# are dynamically created by 'terraform get')
.terraform/*

# ignore local backups
*.tfstate.backup

# ignore timestamped backups
terraform.tfstate.*.backup

# ignore saved-plans
saved-plan
saved-plan.*

# generated by Makefile
environment.sh
